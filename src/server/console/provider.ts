import { Application } from 'chen/console';
import { Provider } from 'chen/core';

/**
 * AppProvider class
 */
export class AppProvider extends Provider {

  /**
   * Register dependencies
   * @param {Application} app
   */
  public async register(app: Application) {

  }
}
